server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1q2w3e4r!!
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  kafka:
    bootstrap-servers: localhost:9092
#    consumer:
#      bootstrap-servers: localhost:9092
#      group-id: groupId  # 식별하기 위함.
#      auto-offset-reset: earliest # 가장 오래된 메세지로 offset reset (오래된 메세지는 삭제) none하면 에러발생
#      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer # key와 value를 역직렬화
#      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
#    producer:
#      bootstrap-servers: localhost:9092
#      key-serializer: org.apache.kafka.common.serialization.StringSerializer
#      value-serializer: org.apache.kafka.common.serialization.StringSerializer
#      StringDeserializer : 여기서는 메시지를 주고받을거라서 StringDeserializer를 사용하지만 JSON데이터를 주고받을거라면 JsonDeserializer 사용